name: CI Pipeline

on:
    push:
        branches:
            - main


jobs:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run tests
        run: cat hello.txt





